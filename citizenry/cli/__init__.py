"""citizenry.cli — operator-facing command-line and tablet tools.

Each module here is an entrypoint, not a runtime component. The runtime
(SurfaceCitizen, mesh transport, EMEX Constitution loader) lives in the
parent ``citizenry`` package.
"""
